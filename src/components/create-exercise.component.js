import React, { Component, Element } from 'react'; // allows us to create components 

export default class CreateExercises extends Element { // all components will start like this
    render() { // all components must render something
        return (
            <div>
                <p> You are on the Create List component!</p>
            </div>
        )
    }
}