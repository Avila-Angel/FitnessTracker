import React, { Component } from 'react'; // allows us to create components 

export default class CreateUsers extends Component { // all components will start like this
    render() { // all components must render something
        return (
            <div>
                <p> You are on the Create Users component!</p>
            </div>
        )
    }
}